{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Narrow capability for the bundled YASC main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
